# Using code completion by pressing the TAB key.
# The completion can be used both for functions and file paths

mydata <- read.csv(file="~/Documents/TeachServ/CLASSES/AGR206/PLS206F10/Examples/Clover.dat")
summary(object= mydata$days)
the.summary.of.days <- summary(mydata$days)
the.summary.of.days
class(the.summary.of.days

########    w  ####### =====
      
      
      manipulate(
        plot(cars, xlim=c(0,x.max)),  
        x.max=slider(15,45))
      
# ===========       
      
      
      #### http://rstudio.org/docs/advanced/manipulate
      #### http://www.statmethods.net/graphs/scatterplot.html